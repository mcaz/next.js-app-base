import { createStyles } from '@/libs/style';

export const s = createStyles({
  PageTemplate__Form: {
    display: 'block',
    width: '100%',
  },
  PageTemplate__Form__Division: {
    display: 'inline-block',
    margin: '0 1.6rem 1.6rem 0',
    verticalAlign: 'top',
  },
});
