import { styles } from '~/libs/style';

export const s = styles({
  List: {
    display: 'block',
    width: '100%',
  },
  ListItem: {
    display: 'inline-block',
    margin: '0 1.6rem 1.6rem 0',
    verticalAlign: 'top',
  },
});
