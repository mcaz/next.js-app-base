import { styles } from '~/libs/style';

export const s = styles({
  // .ResourceTableHeader
  ResourceTableHeader: {
    width: 'auto',
    padding: '1.8rem',
    borderBottom: '0.1rem solid #E0E0E0',
  },
});
