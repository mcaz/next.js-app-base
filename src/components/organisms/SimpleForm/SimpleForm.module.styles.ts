import { createStyles } from "@/libs";

export const s = createStyles(() => {
  return {
    Status: {
      display: 'flex',
      gap: '1rem'
    },
    Status__DD: {
      fontWeight: 700
    },
  };
}, 'ScanDataGrid');
