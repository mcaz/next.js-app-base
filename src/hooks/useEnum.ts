import {
  GridEnum,
  HttpMethodEnum,
  ResponseCodeEnum,
  UserRoleEnum,
} from '@/types/enums';

export const useEnum = () => {
  return {
    GridEnum,
    HttpMethodEnum,
    ResponseCodeEnum,
    UserRoleEnum,
  };
};
