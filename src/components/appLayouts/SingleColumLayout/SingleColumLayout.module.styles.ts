import { createStyles } from "@/libs";

export const s = createStyles({
  Root: {
    width: '100vw',
    maxHeight: '100vh',
  },
  Inner: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    width: '100%',
    height: '100%',
  },
}, 'SingleColumLayout');
