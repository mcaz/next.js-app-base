import { api } from './api';
import { app } from './app';
import { pages } from './pages';
import { storage } from './storage';
import { form } from './form';

export const configs = {
  api,
  app,
  pages,
  storage,
  form,
};
