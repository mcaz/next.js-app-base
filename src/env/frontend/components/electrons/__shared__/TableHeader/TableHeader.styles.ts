import { createStyles } from '@/libs/style';

export const s = createStyles({
  // .TableHeader
  TableHeader: {
    display: 'table-cell',
    wordBreak: 'break-all',
    fontWeight: 'bold',
  },
});
